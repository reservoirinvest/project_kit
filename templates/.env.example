ANTHROPIC_API_KEY=sk-##
GEMINI_API_KEY=AIz##
DEEPSEEK_API_KEY=sk-45##
TAVILY_API_KEY=tvly####
CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-##
# Auto-only fallback link (see rkv-ask/llm_client.py) — OpenRouter's $0
# free-tier catalog. Free signup at openrouter.ai, no top-up needed for
# :free models. Never a legal llm.provider value on its own.
OPENROUTER_API_KEY=sk-or-v##
# Comma-separated model slugs, cheapest/most-available first, sent as
# OpenRouter's native fallback array. Optional — defaults to _FREE_MODELS in
# llm_client.py. Verify current free models at openrouter.ai/models?max_price=0
# before overriding; the free roster rotates.
OPENROUTER_MODELS=
