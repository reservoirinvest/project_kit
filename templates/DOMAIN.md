# DOMAIN.md  (project "memory")
*(Facts, glossary, conventions, and recurring gotchas Claude should NOT have to
re-derive each session. Append as the project teaches you things. This is the
closest honest equivalent to persistent "memory" in a Claude Code project —
it's just a file Claude reads, not a hidden store.)*

## Glossary
*(domain terms specific to this project)*

## External sources / APIs
*(endpoints, quirks, rate limits, auth notes — NO secrets here, those go in .env)*

## Conventions decided over time
*(naming patterns, file layouts, data formats agreed during the build)*

## Known gotchas
*(things that bit us once, so they don't bite again)*
